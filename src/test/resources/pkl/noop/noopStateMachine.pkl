import "modulepath:/pkl/csm/Csml.pkl"

class StateMachine extends Csml.StateMachine {
  name = "stateMachine"
  states {
    new {
      name = "a"
      initial = true
      terminal = true
    }
  }
}